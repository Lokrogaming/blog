const escape = s => String(s ?? '').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
const inline = s => escape(s).replace(/`([^`]+)`/g,'<code>$1</code>').replace(/!\[([^\]]*)\]\((https?:\/\/[^ )]+)\)/g,'<img src="$2" alt="$1">').replace(/\[([^\]]+)\]\((https?:\/\/[^ )]+)\)/g,'<a href="$2" target="_blank" rel="noreferrer">$1</a>').replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>').replace(/\*([^*]+)\*/g,'<em>$1</em>');
export function markdown(markdown='') { let code=false, list=false; const out=[]; for (const raw of markdown.replace(/\r/g,'').split('\n')) { const line=raw.trim(); if (line.startsWith('```')) { if(list){out.push('</ul>');list=false} out.push(code?'</code></pre>':'<pre><code>');code=!code;continue } if(code){out.push(escape(raw)+'\n');continue} if(!line){if(list){out.push('</ul>');list=false}continue} const heading=line.match(/^(#{1,3})\s+(.+)/); if(heading){if(list){out.push('</ul>');list=false}const level=heading[1].length;const text=heading[2];out.push(`<h${level} id="${text.toLowerCase().replace(/[^a-z0-9äöüß]+/gi,'-')}">${inline(text)}</h${level}>`);continue} if(/^[-*]\s+/.test(line)){if(!list){out.push('<ul>');list=true}out.push(`<li>${inline(line.replace(/^[-*]\s+/,''))}</li>`);continue} if(line.startsWith('> ')){out.push(`<blockquote>${inline(line.slice(2))}</blockquote>`);continue}out.push(`<p>${inline(line)}</p>`)} if(list)out.push('</ul>');if(code)out.push('</code></pre>');return out.join(''); }
export function excerpt(markdown='') { return markdown.replace(/[#*_`>-]/g,'').replace(/\[([^\]]+)\]\([^)]*\)/g,'$1').trim().slice(0,170); }
